# bun

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bun` to build the library.

## Running unit tests

Run `nx test bun` to execute the unit tests via [Jest](https://jestjs.io).
